{
  "name": "seguimientopacienteslit",
  "version": "1.0.0",
  "description": "seguimiento de pacientes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-dev-server --node-resolve --open --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@web/dev-server": "^0.3.1",
    "lit": "^2.8.0"
  }
}
